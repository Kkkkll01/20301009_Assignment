<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.order.dao.TransportOrderItemDao">

    <resultMap type="com.example.order.entity.TransportOrderItem" id="TransportOrderItemMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="transportOrderId" column="transport_order_id" jdbcType="INTEGER"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
        <result property="location" column="location" jdbcType="VARCHAR"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TransportOrderItemMap">
        select
          id, transport_order_id, status, location, update_time
        from logistics_order_management.transport_order_item
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TransportOrderItemMap">
        select
          id, transport_order_id, status, location, update_time
        from logistics_order_management.transport_order_item
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="TransportOrderItemMap">
        select
        id, transport_order_id, status, location, update_time
        from logistics_order_management.transport_order_item
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="transportOrderId != null">
                and transport_order_id = #{transportOrderId}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="location != null and location != ''">
                and location = #{location}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into logistics_order_management.transport_order_item(transport_order_id, status, location, update_time)
        values (#{transportOrderId}, #{status}, #{location}, #{updateTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into logistics_order_management.transport_order_item(transport_order_id, status, location, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.transportOrderId}, #{entity.status}, #{entity.location}, #{entity.updateTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into logistics_order_management.transport_order_item(transport_order_id, status, location, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.transportOrderId}, #{entity.status}, #{entity.location}, #{entity.updateTime})
        </foreach>
        on duplicate key update
        transport_order_id = values(transport_order_id) , status = values(status) , location = values(location) ,
        update_time = values(update_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update logistics_order_management.transport_order_item
        <set>
            <if test="transportOrderId != null">
                transport_order_id = #{transportOrderId},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
            <if test="location != null and location != ''">
                location = #{location},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from logistics_order_management.transport_order_item where id = #{id}
    </delete>

</mapper>

