<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.user.dao.UserDao">

    <resultMap type="com.example.user.entity.User" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="account" column="account" jdbcType="VARCHAR"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="email" column="email" jdbcType="VARCHAR"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="role" column="role" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select
          id, account, username, password, email, phone, role, status
        from logistics_order_management.user
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserMap">
        select
          id, account, username, password, email, phone, role, status
        from logistics_order_management.user
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserMap">
        select
        id, account, username, password, email, phone, role, status
        from logistics_order_management.user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="account != null and account != ''">
                and account = #{account}
            </if>
            <if test="username != null and username != ''">
                and username = #{username}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="email != null and email != ''">
                and email = #{email}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="role != null and role != ''">
                and role = #{role}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into logistics_order_management.user(account, username, password, email, phone, role, status)
        values (#{account}, #{username}, #{password}, #{email}, #{phone}, #{role}, #{status})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into logistics_order_management.user(account, username, password, email, phone, role, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.account}, #{entity.username}, #{entity.password}, #{entity.email}, #{entity.phone},
            #{entity.role}, #{entity.status})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into logistics_order_management.user(account, username, password, email, phone, role, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.account}, #{entity.username}, #{entity.password}, #{entity.email}, #{entity.phone},
            #{entity.role}, #{entity.status})
        </foreach>
        on duplicate key update
        account = values(account) , username = values(username) , password = values(password) , email = values(email) ,
        phone = values(phone) , role = values(role) , status = values(status)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update logistics_order_management.user
        <set>
            <if test="account != null and account != ''">
                account = #{account},
            </if>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="email != null and email != ''">
                email = #{email},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="role != null and role != ''">
                role = #{role},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from logistics_order_management.user where id = #{id}
    </delete>

</mapper>

